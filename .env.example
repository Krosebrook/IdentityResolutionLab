# ─────────────────────────────────────────────
# Identity Resolution Lab — Environment Variables
# Copy this file to .env and fill in real values.
# NEVER commit .env to source control.
# ─────────────────────────────────────────────

# Google Gemini API key (required)
# Obtain from https://aistudio.google.com/app/apikey
# ⚠️  This key is embedded in the client bundle and visible to end users.
#     Restrict it to the HTTP referrers of your deployment domain in the
#     Google Cloud console to prevent unauthorised use.
GEMINI_API_KEY=your_gemini_api_key_here
